<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="resources/css/style.css">
  <title>Register</title>
</head>
<body>
  <nav class="navbar">
    <div class="navbar-container">
      <a href="/" class="navbar-brand" id="telesonic">
        <img src="/resources/images/telesonic.png" alt="Telesonic Logo" id="telesonic-logo">
      </a>
      <div class="navbar-links">
          <a href="login" class="navbar-link">Iniciar sesion</a>
      </div>
    </div>
  </nav>
  <div class="register-form">
    <h1>Registrarse</h1>
    <form action="/register" method="POST">
      <label for="user">Usuario</label>
      <input type="text" name="user" required tabindex="1" autofocus>
      <label for="name">Nombre completo</label>
      <input type="text" name="name" required tabindex="2">
      <label for="rol">Rol</label>
      <select id="rol" name="rol" class="select-css" tabindex="3">
        <option value="admin" >Admin</option>
        <option value="data entry" selected>Trabajador</option>
      </select>
      <br>
      <label for="pass">Contraseña</label>
      <input type="password" name="pass" required tabindex="4">
      <div style="display: inline;">
        <br><br>
        <input type="submit" class="btn-save" value="Guardar">
        <a href="/" class="btn-cancel">Cancelar</a>
      </div>
    </form>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@10"></script>
  <% if (typeof alert != "undefined") { %>
    <script>
      Swal.fire({
        title: '<%= alertTitle %>',
        text: '<%= alertMessage %>',
        icon: '<%= alertIcon %>',
        showConfirmButton: <%= showConfirmButton %>,
        timer: <%= timer %>
      }).then(() => {
        window.location = '/<%= ruta %>'
      })
    </script>
  <% } %>
</body>
</html>
